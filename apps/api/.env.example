# Postgres connection string from Neon
# Example: postgresql://user:pass@neon-host/neondb?sslmode=require
DATABASE_URL=postgresql://user:pass@neon-host/neondb?sslmode=require

# Random string (min 32 chars) - generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=your-secret-key-min-32-characters-long

# URL where the API is hosted (without /api/v1)
# Example: http://localhost:8787
BETTER_AUTH_URL=http://localhost:8787

# URL where the frontend is hosted (for CORS and trusted origins)
# Example: http://localhost:3000
FRONTEND_URL=http://localhost:3000
